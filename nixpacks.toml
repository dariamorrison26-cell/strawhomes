[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --prefer-offline"]

[phases.build]
cmds = [
  "rm -rf node_modules/.cache",
  "npm run build"
]

[start]
cmd = "npx serve dist --listen $PORT --no-clipboard"
